{
  "name": "banha-realestate",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node ./scripts/preinstall-check.cjs",
    "dev": "pnpm --filter \"@workspace/api-server\" --filter \"@workspace/banha-realestate\" --parallel run dev",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "build:prod": "pnpm --filter @workspace/banha-realestate run build && pnpm --filter @workspace/api-server run build",
    "start": "node --enable-source-maps ./artifacts/api-server/dist/index.mjs",
    "start:prod": "PORT=${PORT:-3001} node --enable-source-maps ./artifacts/api-server/dist/index.mjs",
    "db:push": "pnpm --filter @workspace/db run push",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "@types/react": "catalog:",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
